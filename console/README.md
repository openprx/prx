# PRX Web Console

Svelte 5 SPA for the PRX gateway API.

## Development

```bash
bun install
bun run dev
```

## Build

```bash
bun run build
```

Build output is written to `dist/`.

## Environment

- `VITE_API_URL` (optional): backend base URL (default: `http://localhost:8080`)
